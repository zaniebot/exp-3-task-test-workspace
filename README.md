# Task Test Workspace

A test workspace for the agent task queue system.

## Scripts

### hello.py
A simple hello-world script with built-in logging support. Run it with:
```bash
python hello.py
```

To enable verbose (debug-level) logging, use the `--verbose` flag:
```bash
python hello.py --verbose
```
